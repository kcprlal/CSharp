public class MartianBase
{
    protected int m_oxygenLevel;
    protected int m_temperature;

    public MartianBase(int oxygenLevel, int temperature)
    {
        m_oxygenLevel = oxygenLevel;
        m_temperature = temperature;
    }
}